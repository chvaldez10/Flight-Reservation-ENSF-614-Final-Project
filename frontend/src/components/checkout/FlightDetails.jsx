import React from "react";
import { Box, Typography } from "@mui/material";

const FlightDetails = ({ flightInfo }) => {
  return (
    <Box>
      <Typography variant="h6">Flight Details:</Typography>
      {/* Display flightInfo details */}
    </Box>
  );
};

export default FlightDetails;
