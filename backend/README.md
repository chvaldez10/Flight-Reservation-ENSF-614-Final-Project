# Back-end

Installation Steps:

`cd backend`

`npm i -g nodemon`

`npm install`

create a `.env` file.

It should be structured like this:
DB_HOST='localhost'
DB_USER='<mysql_user>'
DB_PASSWORD='<mysql_password>'
DB_DATABASE='flight_reservation'
PORT=3001